get:
  operationId: get_block
  parameters:
    - $ref: '/spec/parameters/currency.yaml' 
    - $ref: '/spec/parameters/height.yaml'
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            $ref: '/spec/schemas/block.yaml'
  summary: Get a block by its height
  tags:
  - blocks
  x-openapi-router-controller: openapi_server.controllers.blocks_controller

