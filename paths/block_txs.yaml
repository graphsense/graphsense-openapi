get:
  operationId: list_block_txs
  parameters:
    - $ref: '/spec/parameters/currency.yaml' 
    - $ref: '/spec/parameters/height.yaml'
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            $ref: '/spec/schemas/block_txs.yaml'
  summary: Get all blocks (100 per page)
  tags:
  - blocks
  x-openapi-router-controller: openapi_server.controllers.blocks_controller

