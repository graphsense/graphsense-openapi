get:
  operationId: get_statistics
  parameters:
    - $ref: '/spec/parameters/currency.yaml'
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            $ref: '/spec/schemas/currency.yaml'
  summary: Get statistics of currency
  tags:
  - general
  x-openapi-router-controller: openapi_server.controllers.general_controller

